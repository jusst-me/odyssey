export { sanityClient } from './client';
export { fetchByMarket, fetchSiteSettings } from './queries';
export { projectId, dataset, apiVersion, studioBasePath, assertSanityEnv } from './env';
export {
  MARKETS,
  type Market,
  type Localized,
  type SiteSettings,
  type NavItem,
  type SiteLink,
  type FooterColumn,
} from './types';
